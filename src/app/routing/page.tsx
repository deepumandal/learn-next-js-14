import React from "react";

const Routing = () => {
  return (
    <div>
      <h1 className=" text-2xl my-3">Routing</h1>
      <p>Nextjs has file system based Routing</p>
      <p>
        urls paths that users can access in the browser are define by files and
        folders in codebases
      </p>

      <h1 className=" text-2xl my-3">Routing Convensions</h1>
      <p>all routes must be places in app folder</p>
    
    </div>
  );
};

export default Routing;
